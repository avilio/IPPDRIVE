/**
 * Class that saves data of the logged user
 */
class PaeUser{
  /**
   * username logged
   */
  String username;

  /**
   * session of the user logged
   */
  String session;

  PaeUser(this.username, this.session);

}